// Saat halaman selesai dimuat, animasi akan berjalan (bisa ditambah interaktif jika perlu)
document.addEventListener("DOMContentLoaded", () => {
  const logo = document.querySelector('.logo-bso');
  logo.classList.add('fade-in-top');
});
